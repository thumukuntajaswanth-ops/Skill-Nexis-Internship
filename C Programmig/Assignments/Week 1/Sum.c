#include <stdio.h>
int main(){
    int a,b,sum;
    printf("Enter the value of A:");
    scanf("%d",&a);
    printf("enter the value of B:");
    scanf("%d",&b);
    sum=a+b;
    printf("the sum the two numbers is %d",sum);
    return 0;
}